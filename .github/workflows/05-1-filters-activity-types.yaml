name: 05 -1 - Evenet Filters and Activity Types

on:
    pull request:
        types:
            - opened
            - synchronize
        branches:
            - main
            
jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Running whenever a PR is opened or synchronized And base branch is main"